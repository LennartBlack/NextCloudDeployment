apiVersion: v1
kind: Service
metadata:
  name: nextcloud-mariadb          # must match .spec.serviceName of the StatefulSet
  namespace: nextcloud
spec:
  clusterIP: None                  # headless for stable DNS
  selector:
    app: nextcloud-mariadb
  ports:
    - name: mysql
      port: 3306
      targetPort: mysql

